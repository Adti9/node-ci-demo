{
  "name": "node-ci-demo",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "node test.js",
    "build": "echo 'Building application...' && mkdir -p dist && echo 'Build Artifact' > dist/app.txt"
  }
}
